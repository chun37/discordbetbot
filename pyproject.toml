[project]
name = "discordbetbot"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "discord.py>=2.7.1,<3",
    "aiosqlite>=0.22.1",
    "python-dotenv>=1.0.0",
    "matplotlib>=3.8",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]